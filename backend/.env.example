# Backend Production Environment Variables
# Optional custom dataset directory path (defaults to backend/data)
NARCODES_DATA_DIR=data

# Comma-separated list of allowed CORS origins (defaults to *)
CORS_ORIGINS=*

# Port for ASGI server
PORT=8000
